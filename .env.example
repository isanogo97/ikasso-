# Environment variables example for Ikasso

# Base URL of the app (used for absolute links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment (do not change at runtime)
NODE_ENV=development

# Add API keys or secrets here (never commit real secrets)
# EXAMPLE_API_KEY=

